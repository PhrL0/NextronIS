# 🚀 Dependências do projeto NextronIS

# Inicialização do projeto
npm init -y

# 🧠 TypeScript + compilador
npm install typescript ts-node --save-dev

# 🧰 Fastify (Framework HTTP)
npm install fastify

# 📡 Cliente MQTT
npm install mqtt

# 🌐 WebSocket com 'ws'
npm install ws

# 📚 Tipagens para desenvolvimento com TypeScript
npm install @types/node @types/ws --save-dev

# 🧹 Utilitários recomendados (opcional, mas úteis)
npm install dotenv # para variáveis de ambiente
npm install nodemon --save-dev # para reinício automático do servidor

# Core do servidor
fastify
fastify-websocket

# MQTT client
mqtt

# Integração com a API Gemini (Google Generative AI)
@google/generative-ai

# Tipagem e suporte para TypeScript
typescript
ts-node
@types/node

# Variáveis de ambiente
dotenv

# (Opcional) Linter e qualidade de código
eslint
prettier

# (Se estiver usando ESModules ou import/export mesmo fora de React)
tsconfig-paths

#Para subir o container do REDIS
docker run -d --name my-redis -p 6379:6379 redis


npx prisma migrate dev --name init


Nosso sistema aplica o modelo de Multi-Armed Bandit, onde cada conjunto de hiperparâmetros da IA é tratado como um "braço" em um jogo de escolhas adaptativas.

O algoritmo Thompson Sampling atua como uma inteligência que simula milhares de futuros possíveis a cada nova interação. Ele estima probabilisticamente qual braço — ou seja, qual configuração da IA — tem maior chance de oferecer a melhor resposta, com base em feedbacks anteriores.

Essa abordagem garante um equilíbrio dinâmico entre exploração e exploração: testamos novas possibilidades, mas priorizamos aquelas com histórico promissor.

Como resultado, o sistema se torna autoadaptável: ele aprende não só com o acerto, mas com a incerteza — e transforma cada feedback em uma peça da arquitetura de decisão futura.





INSERT INTO hyperParameterArm (
    id, modelName, version, temperature, topP, topK, maxOutputTokens, responseMimeType,
    successes, failures, createdAt, updatedAt
) VALUES
(
    '8a438c71-9191-40c7-8bbb-640927fc6607', 'Gemini', '1.0', 0.7, 0.9, 50, 1024, 'text/plain',
    3, 0, '2025-06-02 17:45:04', '2025-06-02 17:58:05'
),
(
    'a7f9e931-bf38-4a1c-bbe2-533cc8793f2a', 'Gemini', '1.1', 0.5, 0.85, 40, 512, 'application/json',
    10, 2, '2025-06-01 15:22:10', '2025-06-02 10:11:35'
),
(
    'c9e8f8e4-3f97-4ee9-8d0d-7fa43bc2bb29', 'Gemini', '2.1-beta', 0.65, 0.8, 55, 1024, 'text/plain',
    7, 3, '2025-06-02 08:00:00', '2025-06-02 12:00:00'
),
(
    'b4d5e53a-2e5a-423d-9271-7c9d0a872f3b', 'Gemini', '2.2', 0.8, 0.9, 45, 1500, 'application/json',
    5, 1, '2025-06-01 11:30:00', '2025-06-01 18:45:00'
);


select * from interacaoia;
select id,successes,failures from hyperparameterarm;
